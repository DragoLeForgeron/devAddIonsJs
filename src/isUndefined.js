/**
 * Return if a varaible is undefined.
 * @param {any} variable - Any variable to know if it undefined.
 * @returns {Boolean} Boolean
 */
export function isUndefined(variable) {
    return typeof variable === 'undefined';
}